/** Wheel filename from `uv build` in the Python package root (parent of `github-io/`); keep version in sync. */
export const LOCAL_PCB_WHEEL_FILE = "cadbuildr_electronics-0.1.0-py3-none-any.whl";

/** URL path segment (under Vite `base`, e.g. `/repo/local-pcb/...` on GitHub Pages). */
export const LOCAL_PCB_URL_SEGMENT = "local-pcb";
